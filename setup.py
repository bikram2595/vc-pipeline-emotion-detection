from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='emotion detection project with VC and ML pipeline',
    author='bikram',
    license='',
)
